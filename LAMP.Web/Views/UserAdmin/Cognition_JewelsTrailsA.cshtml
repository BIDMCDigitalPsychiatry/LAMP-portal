@model CognitionJewelsTrailsAViewModel
@using System.Globalization;
@using PagedList.Mvc;
@{
    ViewBag.Title = "Cognition_JewelsTrailsA";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="content-wrapper">
    <div class="title-sub clearfix">
        <h4>Cognition</h4>
        <button class="btn btn-primary back" title="Back" onclick="JavaScript: window.history.back(1); return false;">back</button>
    </div>
    @using (Ajax.BeginForm("Cognition_JewelsTrailsA", "UserAdmin", new AjaxOptions() { HttpMethod = "GET", UpdateTargetId = "divsurveyCognition", InsertionMode = InsertionMode.Replace }, new { id = "companyListForm", @class = "form-horizontal" }))
    {
        <div class="main-content">
            <h5>Jewels Trails A</h5>
            <div class="table-responsive survey cognition" id="divCTestTrailB">
                @{ Html.RenderPartial("_Cognition_JewelsTrailsAListPartial", Model); }
            </div>
            <div class="status-wrap clearfix">
                <div class="date game-detail">
                    <p>last cognition Date: <strong>@Model.LastCognitionDate.ToString("MM/dd/yyyy").Replace('-', '/')</strong></p>
                    <p>Total Bonus Collected: <strong>@Model.TotalBonusCollected</strong></p>
                    <p>Total Jewels Collected: <strong>@Model.TotalJewelsCollected</strong></p>
                </div>
                <div class="time"><p>Last Time Taken (Hours): <strong>@Model.DurationString</strong></p> <p class="rating">Total Rating: <span>_</span></p></div>
            </div>
        </div>
    }
</div>


